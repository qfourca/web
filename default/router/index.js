const express = require('express');

const router = express.Router();

router.get('/', async (req, res) => {
    // console.log(req.url);
    // if (req.url == '/') {
    //     req.url = 'index.html';
    // }
    // var filename = path.join(process.cwd(), '/src', req.url);
    //res.sendFile(filename);
    console.log(input);
});

module.exports = router;